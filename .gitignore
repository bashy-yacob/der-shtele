# deps
node_modules/
.pnp
.pnp.js

# build
.next/
out/
dist/
build/

# env
.env
.env.local
.env.*.local
backend/.env
frontend/.env
frontend/.env.local

# prisma
backend/prisma/*.db

# uploads (local dev fallback — production uses Supabase Storage)
frontend/public/uploads/
backend/uploads/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

# vercel
.vercel

# editor
.idea/

# design-lab (local design-variation preview galleries — not part of the app)
design-lab/
# playwright-mcp scratch output (snapshots/console/screenshots)
.playwright-mcp/

# design-sync (staged scripts, build output, machine state, build inputs)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
frontend/.ds-sync-build/
