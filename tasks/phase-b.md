# שלב ב — CRM פנימי

> דרישת קדם: שלב א הושלם ועולה בפרודקשן

## כניסה למערכת
- [ ] דף login — `/admin/login`
- [ ] אימות עם JWT (jose) או next-auth
- [ ] middleware — הגנה על כל routes תחת `/admin`
- [ ] `requireAdmin()` ב-`lib/auth.ts` — מימוש אמיתי
- [ ] logout — ניקוי token

## ניהול מועמדים
- [ ] רשימת מועמדים עם חיפוש וסינון לפי סטטוס / תחום / אזור
- [ ] דף מועמד — כל הפרטים, סטטוס, הערות, קורות חיים
- [ ] שינוי סטטוס — דרך `assertCandidateTransition()` בלבד
- [ ] הוספת הערה פנימית
- [ ] הורדת קורות חיים מ-Supabase Storage
- [ ] רשימת משרות שהוגש אליהן

## ניהול שיחות (Call Log)
- [ ] הוספת רשומת שיחה — תאריך, מי התקשר, סיכום
- [ ] תזכורת לשיחה חוזרת — `followUpAt`
- [ ] היסטוריית שיחות ממוינת לפי תאריך

## ניהול מעסיקים
- [ ] רשימת מעסיקים
- [ ] טופס הוספת מעסיק — שם, ח.פ, איש קשר, טלפון, מייל
- [ ] עריכת פרטי מעסיק
- [ ] הערות פנימיות (אמינות, העדפות)
- [ ] היסטוריית משרות ממעסיק

## ניהול משרות (פנימי)
- [ ] טופס הוספת משרה — שדות ציבוריים + פנימיים
- [ ] שדה `descriptionPublic` — מה שעולה לאתר
- [ ] שדה `descriptionInternal` — דרישות מלאות
- [ ] קישור למעסיק
- [ ] שינוי סטטוס — דרך `assertJobTransition()`
- [ ] אישור רבני — תיוג + שם המאשר
- [ ] הפעלה / השהיה / סגירה של משרה
- [ ] פרסום לאתר הציבורי בלחיצה

## הצגת מועמדים למשרות
- [ ] בחירת מועמדים מתאימים ממאגר
- [ ] יצירת `JobPresentation` — מניעת כפילויות
- [ ] עדכון סטטוס המועמד ביחס למשרה
- [ ] רשימת מועמדים שהוצגו לכל משרה

## API Routes — שלב ב (כולם מוגנים ב-requireAdmin)
- [ ] `GET /api/admin/candidates` — רשימה עם פילטרים
- [ ] `GET /api/admin/candidates/[id]` — מועמד מלא
- [ ] `PATCH /api/admin/candidates/[id]/status` — שינוי סטטוס
- [ ] `POST /api/admin/candidates/[id]/calls` — הוספת שיחה
- [ ] `GET /api/admin/employers` — רשימת מעסיקים
- [ ] `POST /api/admin/employers` — הוספת מעסיק
- [ ] `PATCH /api/admin/employers/[id]` — עריכה
- [ ] `GET /api/admin/jobs` — רשימת משרות פנימית
- [ ] `POST /api/admin/jobs` — הוספת משרה
- [ ] `PATCH /api/admin/jobs/[id]/status` — שינוי סטטוס
- [ ] `POST /api/admin/jobs/[id]/present` — הצגת מועמד
